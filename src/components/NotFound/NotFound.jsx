import React from 'react';
import './NotFound.css';

const NotFound = () => {
    return (
        <div className='not-found'>
            <h2>Page doesn't found</h2>
        </div>
    );
};

export default NotFound;